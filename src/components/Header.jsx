import React from "react";
import MenuBookIcon from "@material-ui/icons/MenuBook";

const Header = () => {
    return (
        <header>
            <h1>
                <MenuBookIcon />
                Note-it!
            </h1>
        </header>
    );
};

export default Header;
